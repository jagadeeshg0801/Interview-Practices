import { HighlightDirectDirective } from './highlight-direct.directive';

describe('HighlightDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDirectDirective();
    expect(directive).toBeTruthy();
  });
});
