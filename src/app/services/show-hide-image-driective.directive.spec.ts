import { ShowHideImageDriectiveDirective } from './show-hide-image-driective.directive';

describe('ShowHideImageDriectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHideImageDriectiveDirective();
    expect(directive).toBeTruthy();
  });
});
