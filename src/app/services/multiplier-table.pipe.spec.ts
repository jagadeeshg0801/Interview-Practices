import { MultiplierTablePipe } from './multiplier-table.pipe';

describe('MultiplierTablePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplierTablePipe();
    expect(pipe).toBeTruthy();
  });
});
